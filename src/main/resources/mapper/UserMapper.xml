<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.guet.cantin.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="cn.edu.guet.cantin.domain.User">
        <id column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="register_time" property="registerTime" jdbcType="TIMESTAMP"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        user_id, username, phone, email, password, avatar, register_time, status
    </sql>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE email = #{email}
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE phone = #{phone}
        AND status = 1
    </select>

    <!-- 检查邮箱是否已存在 -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE email = #{email}
    </select>

    <!-- 检查手机号是否已存在 -->
    <select id="countByPhone" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE phone = #{phone}
    </select>

    <!-- 获取用户统计信息 -->
    <select id="getUserStatistics" resultType="java.util.Map">
        SELECT
            COUNT(*) as total_users,
            COUNT(CASE WHEN status = 1 THEN 1 END) as active_users,
            COUNT(CASE WHEN status = 0 THEN 1 END) as disabled_users
        FROM user
    </select>

    <!-- 根据用户名或邮箱查询用户 -->
    <select id="selectByUsernameOrEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user
        WHERE (username = #{usernameOrEmail} OR email = #{usernameOrEmail})
    </select>

</mapper>

